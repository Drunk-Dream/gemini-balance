# Google Gemini API 密钥。
# 可以使用逗号分隔的单个环境变量：GOOGLE_API_KEYS="key1,key2"
# 或者使用索引化的多个环境变量（推荐）：
# GOOGLE_API_KEY_1="your_api_key_1"
# GOOGLE_API_KEY_2="your_api_key_2"
# GOOGLE_API_KEY_3="your_api_key_3"
GOOGLE_API_KEYS=

# Google Gemini API 的基础 URL。
# 默认值：https://generativelanguage.googleapis.com
GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com

# OpenAI API 的基础 URL。
# 默认值：https://generativelanguage.googleapis.com (注意：这里可能需要根据实际OpenAI API地址调整)
OPENAI_API_BASE_URL=https://generativelanguage.googleapis.com

# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)。
# 默认值：INFO
LOG_LEVEL=INFO

# 是否启用调试日志。设置为 "true" 启用，"false" 禁用。
# 默认值：False
DEBUG_LOG_ENABLED=False

# 调试日志文件的路径。
# 默认值：logs/gemini_debug.log
DEBUG_LOG_FILE=logs/gemini_debug.log

# API 密钥冷却时间（秒），当密钥失败时，该密钥将在此时间内不被使用。
# 默认值：300 (5分钟)
API_KEY_COOL_DOWN_SECONDS=300

# API 密钥失败阈值，当密钥失败次数达到此值时，将进入冷却期。
# 默认值：3
API_KEY_FAILURE_THRESHOLD=3

# API 密钥最大冷却时间（秒），防止无限期冷却。
# 默认值：43200 (12小时)
MAX_COOL_DOWN_SECONDS=43200

# 默认的等待时间（秒），当 API 请求被限速时，将等待此时间后重试。
# 默认值：90 (1.5分钟)
RATE_LIMIT_DEFAULT_WAIT_SECONDS=90

# 日志历史记录大小，用于在内存中保留的日志条目数。
# 默认值：100
LOG_HISTORY_SIZE=100

# Redis 配置
REDIS_HOST=gemini-balance-redis
REDIS_PORT=6379
REDIS_DB=0

# 是否强制重置 Redis 数据库。
FORCE_RESET_REDIS=False

# 最大重试次数。
# 默认值：3
MAX_RETRIES=3
